configure([project(':shared'), project(':server'), project(':client')])
{
    apply plugin: 'java'

    group = 'blue.made.angle'
    version = '0.0-SNAPSHOT'
	
	repositories 
	{
		mavenCentral()
		mavenLocal()
	}
}